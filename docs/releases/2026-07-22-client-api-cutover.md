# Client API cutover — 2026-07-22

## Production targets

- API: `https://reflexion.production.tktonny.top`
- MongoDB database: `reflexion_production`
- Atlas cluster host: `ref.5l9bxw9.mongodb.net`
- Data region: `ap-southeast-1`

Both Android clients compile the same API domain. Provider account keys and the MongoDB connection
string remain server-only.

## Android artifacts

| Client | Version | Package | SHA-256 |
| --- | --- | --- | --- |
| Caregiver | `1.0.1` (`versionCode 2`) | `com.tarinpairor.reflexionapp` | `65e6ee6d317c2357e722adf15b7a30fad6258843f086cd0aa2dade22e817f3d5` |
| Mirror | `1.0.1` (`versionCode 3`) | `com.reflexion.mirror` | `947fc83d5878e162eb0db563215425a0afa652bcae96183586a8fbe93aa750d7` |

The APK files are kept in each app's ignored `dist-apks/` directory. Bundle inspection confirmed the
production API URL is present and the retired Vercel URL, MongoDB URI, server Qwen credentials, and
the insecure client-key flag are absent.

## Acceptance evidence

Public unauthenticated deployment smoke checks passed for health, security headers, the `/api/v1`
authentication boundary, and the error envelope.

The authenticated production flow passed under run
`deployment-smoke-1784721418216-5c1e8cad`:

1. Administrator login
2. Patient creation and monitoring consent
3. Device provisioning
4. Pairing v2 and device credential exchange
5. Authenticated heartbeat
6. Session creation and realtime ticket issuance
7. Event-batch ingestion and session completion
8. Worker processing to `completed` / `include` / `baseline_building`

The one-time smoke records and credentials were removed after the run.

## Provisioning note

An upgrade installation on an already-paired Mirror retains its device credentials. A factory-new or
cleared device still needs its own bootstrap credential generated by `provision:device`; a shared
bootstrap token must never be compiled into a general release APK.
