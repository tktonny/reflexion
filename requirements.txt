# Python runtime dependencies only.
# System binaries are also required in production:
# - ffmpeg and ffprobe for browser-recorded video standardization and compression
# - These are not installed by pip and must be provided by the host OS
fastapi>=0.115.0
httpx>=0.27.0
numpy>=1.26.0
opencv-python-headless>=4.10.0
pydantic>=2.7.0
python-multipart>=0.0.9
scikit-image>=0.24.0
uvicorn>=0.30.0
websockets>=15.0.1
