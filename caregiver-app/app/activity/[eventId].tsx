import { useLocalSearchParams, useRouter } from 'expo-router';
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { AppHeader, ProvenanceSection, ScreenLayout, TertiaryButton } from '../../src/components/AppUI';
import { colors, fontFamily, fontSize, radius, spacing } from '../../src/theme';
export default function ActivityDetailScreen() { const { eventId } = useLocalSearchParams<{ eventId: string }>(); const router = useRouter(); const title = eventId === 'conversation' ? 'Conversation recorded' : eventId === 'routine-response' ? 'Reminder response recorded' : 'Activity detail'; return <ScreenLayout><AppHeader title="Activity" onBack={() => router.back()} /><Text accessibilityRole="header" style={styles.title}>{title}</Text><View style={styles.card}><ProvenanceSection label="Observed">Recorded today at 9:18 AM.</ProvenanceSection><ProvenanceSection label="Detail">This activity is shown as a factual record. It does not make a health or safety claim.</ProvenanceSection><ProvenanceSection label="Limitations">Some detail may be unavailable if a device connection or permission was restricted.</ProvenanceSection></View><TertiaryButton label="Back to activity" onPress={() => router.back()} /></ScreenLayout>; }
const styles = StyleSheet.create({ title: { color: colors.text.primary, fontFamily: fontFamily.display, fontSize: fontSize.title, fontWeight: '500', marginTop: spacing.xl }, card: { backgroundColor: colors.surface.card, borderColor: colors.border.default, borderRadius: radius.xl, borderWidth: 1, overflow: 'hidden', paddingHorizontal: spacing.lg } });
