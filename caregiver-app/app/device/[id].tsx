import { useLocalSearchParams, useRouter } from 'expo-router';
import React, { useState } from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { AppHeader, ChoiceCard, PrimaryButton, ScreenLayout } from '../../src/components/AppUI';
import { colors, fontFamily, fontSize, spacing } from '../../src/theme';
const DEVICES = [{ icon: 'monitor' as const, title: 'Mirror', description: 'Reflexion Mirror' }, { icon: 'heart' as const, title: 'Bear', description: 'Reflexion Companion Bear' }, { icon: 'smartphone' as const, title: 'App', description: 'Reflexion Mobile App' }, { icon: 'tablet' as const, title: 'Other supported device', description: 'A compatible tablet or display' }];
export default function DeviceScreen() { const router = useRouter(); const { id } = useLocalSearchParams<{ id: string }>(); const [selected, setSelected] = useState(0); return <ScreenLayout><AppHeader title="Connected device" onBack={() => router.back()} /><Text accessibilityRole="header" style={styles.title}>Select person and device type</Text><Text style={styles.subtitle}>This device is for Mum. Choose the Reflexion device you would like to connect.</Text><View style={styles.cards}>{DEVICES.map((device, index) => <ChoiceCard key={device.title} {...device} selected={index === selected} onPress={() => setSelected(index)} />)}</View><PrimaryButton label="Choose pairing method" onPress={() => router.push(`/device/${id}/pairing`)} /></ScreenLayout>; }
const styles = StyleSheet.create({ title: { color: colors.text.primary, fontFamily: fontFamily.display, fontSize: fontSize.title, fontWeight: '500', marginTop: spacing.xl }, subtitle: { color: colors.text.secondary, flexShrink: 1, fontSize: fontSize.body, lineHeight: 22 }, cards: { gap: spacing.md, marginBottom: spacing.lg, marginTop: spacing.md } });
