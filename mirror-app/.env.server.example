# Local relay diagnostics only. Copy to .env.server.local; never load this file into an Expo build.
QWEN_API_KEY=replace-with-server-only-qwen-key
MONGODB_URI=mongodb+srv://replace-with-server-only-atlas-uri
REFLEXION_RELAY_PORT=8787
