.expo/
node_modules/
dist/
build/
.env
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Release APKs (large, built locally)
dist-apks/
dist-linux/
# Staged OTA payloads (bundle zips + AppImages) — rsynced to the update host, never committed
dist-updates/
